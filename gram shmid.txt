###orthogonal/normal set of vectors in r3
a = np.array([2/math.sqrt(2), 0, -1/math.sqrt(2)])
b = np.array([1/2, math.sqrt(2)/2, 1/2])
c = np.array([1/2, (math.sqrt(2)/-2), 1/2])

#### linearly independent set of vectors in r3
a = np.array([1, 10, -5])
b = np.array([4, 2, 0])
c = np.array([0, 1, 6])

#### linearly independent set of vectors in r3
a = np.array([4, 3, 1])
b = np.array([0, 2, 1])
c = np.array([0, 3, 1])